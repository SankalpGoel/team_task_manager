[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: tests that require a Postgres TEST_DATABASE_URL (skipped otherwise)
